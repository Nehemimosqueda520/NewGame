const Player_Movements = {
  x: 200,
  y: 500,
};

const shapeDelay = 3000;

const TRIANGULE = "triangle";
const SQUARE = "square";
const ROMBO = "rombo";
const shapes = [TRIANGULE, SQUARE, ROMBO];

export { Player_Movements, shapeDelay, shapes, TRIANGULE, SQUARE, ROMBO };
